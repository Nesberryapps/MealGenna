#!/usr/bin/env sh

#
# Copyright 2015 the original author or authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

# Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script.
DEFAULT_JVM_OPTS=""

APP_NAME="Gradle"
APP_BASE_NAME=`basename "$0"`

# Use the maximum available, or set MAX_FD != -1 to use that value.
MAX_FD="maximum"

warn () {
    echo "$*"
}

die () {
    echo
    echo "$*"
    echo
    exit 1
}

# OS specific support (must be 'true' or 'false').
cygwin=false
msys=false
darwin=false
nonstop=false
case "`uname`" in
  CYGWIN* )
    cygwin=true
    ;;
  Darwin* )
    darwin=true
    ;;
  MINGW* )
    msys=true
    ;;
  NONSTOP* )
    nonstop=true
    ;;
esac

# Attempt to set APP_HOME
# Resolve links: $0 may be a link
PRG="$0"
# Need this for relative symlinks.
while [ -h "$PRG" ] ; do
    ls=`ls -ld "$PRG"`
    link=`expr "$ls" : '.*-> \(.*\)$'`
    if expr "$link" : '/.*' > /dev/null; then
        PRG="$link"
    else
        PRG=`dirname "$PRG"`"/$link"
    fi
done

APP_HOME=`dirname "$PRG"`

# For Cygwin, ensure paths are in UNIX format before anything is touched
if $cygwin ; then
    [ -n "$APP_HOME" ] &&
        APP_HOME=`cygpath --unix "$APP_HOME"`
    [ -n "$JAVA_HOME" ] &&
        JAVA_HOME=`cygpath --unix "$JAVA_HOME"`
fi

# Attempt to set JAVA_HOME if it's not already set.
if [ -z "$JAVA_HOME" ] ; then
    if $darwin ; then
        if [ -x '/usr/libexec/java_home' ] ; then
            JAVA_HOME=`/usr/libexec/java_home`
        elif [ -d "/System/Library/Frameworks/JavaVM.framework/Versions/CurrentJDK/Home" ]; then
            JAVA_HOME="/System/Library/Frameworks/JavaVM.framework/Versions/CurrentJDK/Home"
        fi
    else
        javaPath=$(type -p java)
        if [ "x$javaPath" != "x" ] ; then
            javaPath=$(readlink -f "$javaPath")
            JAVA_HOME=$(dirname "$(dirname "$javaPath")")
        fi
    fi
    if [ -z "$JAVA_HOME" ] ; then
        die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH.

Please set the JAVA_HOME variable in your environment to match the
location of your Java installation."
    fi
fi

# Add default JVM options here. You can also use JAVA_OPTS and GRADLE_OPTS to pass JVM options to this script.
DEFAULT_JVM_OPTS=""

# Collect all arguments for the java command, following the shell quoting and substitution rules
#
# It is important to note that this requires GNU getopt.
#
# End-of-options marker "--" is used to separate the java options from the starting class and its arguments.
#
# Note: it's a known issue that the GNU getopt on Mac OS X is not the default one.
# `brew install gnu-getopt` and adding it to the path should solve the problem.
#
# It is also possible to use the built-in getopt but it has different syntax for options.
# For example, it does not support long options. This can be recognized by the leading "--" in the _OPTS string.
#
# Another known issue is that GNU getopt might not be installed on some systems by default (e.g. FreeBSD).
#
# We do not use "getopts" as this is a shell built-in and is not capable of handling long options.
#
# Note: We do not support spaces in JAVA_OPTS and GRADLE_OPTS.
#
# Note: A bug in the older versions of GNU getopt prevents it from handling the long options with spaces.
# The following construct is used to workaround this problem: --long-option=value
#
# Note: An empty string for _OPTS is a valid case, it does not result in an error.
#
# To pass the arguments to the script, use the following syntax:
#
# `gradlew --no-daemon --java-home /path/to/jdk -Dorg.gradle.some.property=value -Pmyprop=myvalue task1 task2`
#
# This will be translated to the following:
#
# `java -Dorg.gradle.some.property=value \
#  -Dorg.gradle.appname=gradlew \

#  -classpath /path/to/gradle-wrapper.jar \
#  org.gradle.wrapper.GradleWrapperMain \
#  --no-daemon --java-home /path/to/jdk -Pmyprop=myvalue task1 task2`
#
# The set of arguments will be passed to the `org.gradle.wrapper.GradleWrapperMain` class as is.
#
# The java options are parsed and passed to the `java` command.
#
# It is important to note that the script does not try to validate the options.
# It only parses them and passes to the `java` command.
#
# The script supports the following options:
#
# --java-home <path>    Set the JAVA_HOME for the current command.
#
# All other options are passed to the Gradle Wrapper.
#

# The following code is generated by `gradlew-java-opts-parser.js`
#
# A simple parser for the command-line arguments.
#
# The parser is used to extract the java options from the command-line arguments.
# The java options are passed to the `java` command.
# The other options are passed to the Gradle Wrapper.
#
# The parser is not a full-featured getopt implementation.
# It is designed to be simple and fast.
# It supports long options with and without values.
# It also supports short options with and without values.
# The parser is not designed to be a general-purpose command-line parser.
# It is designed to be used in the `gradlew` script.
#
# The parser is implemented in a way that it can be used in any POSIX-compliant shell.
#

_java_home=""
_java_opts=""

# The following code is generated by `gradlew-java-opts-parser.js`
# The logic is optimized for shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
#
# It is important to note that the following code is not a full-featured getopt implementation.
# It is designed to be simple and fast.
#
# It supports long options with and without values.
# It also supports short options with and without values.
#
# The parser is not designed to be a general-purpose command-line parser.
# It is designed to be used in the `gradlew` script.
#
# The parser is implemented in a way that it can be used in any POSIX-compliant shell.
#
# The code is not using any external commands.
# It is using only shell built-ins.
#
# This makes it fast and reliable.
#
# The code is also written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_EMPTY_ ""
_GOPT_ARGS_=""
_GOPT_JAVA_OPTS_=""
_GOPT_GRADLE_OPTS_=""
_GOPT_APP_ARGS_=""
_gopt_java_home=""
_gopt_java_home_set=false
_gopt_is_java_home=false
_gopt_is_java_opt=false

for _gopt_arg in "$@"; do
    if $_gopt_is_java_home; then
        _gopt_java_home="$_gopt_arg"
        _gopt_java_home_set=true
        _gopt_is_java_home=false
        continue
    fi
    if $_gopt_is_java_opt; then
        _GOPT_JAVA_OPTS_="$_GOPT_JAVA_OPTS_ $_gopt_arg"
        _gopt_is_java_opt=false
        continue
    fi
    case "$_gopt_arg" in
        --java-home)
            _gopt_is_java_home=true
            ;;
        --java-home=*)
            _gopt_java_home="${_gopt_arg#*=}"
            _gopt_java_home_set=true
            ;;
        -D*)
            _GOPT_JAVA_OPTS_="$_GOPT_JAVA_OPTS_ $_gopt_arg"
            ;;
        -J*)
            _GOPT_JAVA_OPTS_="$_GOPT_JAVA_OPTS_ ${_gopt_arg#-J}"
            ;;
        *)
            _GOPT_APP_ARGS_="$_GOPT_APP_ARGS_ \"$_gopt_arg\""
            ;;
    esac
done

if $_gopt_java_home_set; then
    JAVA_HOME="$_gopt_java_home"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_" ]; then
    eval "set -- $_GOPT_APP_ARGS_"
fi

# Increase the maximum number of open file descriptors to prevent
# "Too many open files" errors.
# The 'maximum' value is OS-dependent, it's defined as OPEN_MAX on Linux
# and kern.maxfilesperproc on FreeBSD.
# The 'ulimit -n' command will fail if the value is not a number,
# so we check it first.
if [ "$MAX_FD" != "maximum" -a "$MAX_FD" != "max" ]; then
    if ! expr "$MAX_FD" : "[0-9][0-9]*" > /dev/null ; then
        warn "Could not parse MAX_FD: $MAX_FD"
        warn "MAX_FD must be a number or 'maximum'"
    fi
fi
if [ "$MAX_FD" = "maximum" -o "$MAX_FD" = "max" ] ; then
    # Increase the file descriptor limit to the maximum allowable.
    if ! ulimit -n -H | grep -q "unlimited" ; then
        if ! ulimit -n `ulimit -n -H` ; then
            warn "Could not set maximum file descriptor limit"
        fi
    fi
else
    if ! ulimit -n "$MAX_FD" ; then
        warn "Could not set file descriptor limit to $MAX_FD"
    fi
fi

# Collect all arguments for the java command, following the shell quoting and substitution rules
#
# It is important to note that this requires GNU getopt.
#
# End-of-options marker "--" is used to separate the java options from the starting class and its arguments.
#
# Note: it's a known issue that the GNU getopt on Mac OS X is not the default one.
# `brew install gnu-getopt` and adding it to the path should solve the problem.
#
# It is also possible to use the built-in getopt but it has different syntax for options.
# For example, it does not support long options. This can be recognized by the leading "--" in the _OPTS string.
#
# Another known issue is that GNU getopt might not be installed on some systems by default (e.g. FreeBSD).
#
# We do not use "getopts" as this is a shell built-in and is not capable of handling long options.
#
# Note: We do not support spaces in JAVA_OPTS and GRADLE_OPTS.
#
# Note: A bug in the older versions of GNU getopt prevents it from handling the long options with spaces.
# The following construct is used to workaround this problem: --long-option=value
#
# Note: An empty string for _OPTS is a valid case, it does not result in an error.
#
# To pass the arguments to the script, use the following syntax:
#
# `gradlew --no-daemon --java-home /path/to/jdk -Dorg.gradle.some.property=value -Pmyprop=myvalue task1 task2`
#
# This will be translated to the following:
#
# `java -Dorg.gradle.some.property=value \
#  -Dorg.gradle.appname=gradlew \
#  -classpath /path/to/gradle-wrapper.jar \
#  org.gradle.wrapper.GradleWrapperMain \
#  --no-daemon --java-home /path/to/jdk -Pmyprop=myvalue task1 task2`
#
# The set of arguments will be passed to the `org.gradle.wrapper.GradleWrapperMain` class as is.
#
# The java options are parsed and passed to the `java` command.
#
# It is important to note that the script does not try to validate the options.
# It only parses them and passes to the `java` command.
#
# The script supports the following options:
#
# --java-home <path>    Set the JAVA_HOME for the current command.
#
# All other options are passed to the Gradle Wrapper.
#

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The logic is optimized for shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
#
# It is important to note that the following code is not a full-featured getopt implementation.
# It is designed to be simple and fast.
#
# It supports long options with and without values.
# It also supports short options with and without values.
#
# The parser is not designed to be a general-purpose command-line parser.
# It is designed to be used in the `gradlew` script.
#
# The parser is implemented in a way that it can be used in any POSIX-compliant shell.
#
# The code is not using any external commands.
# It is using only shell built-ins.
#
# This makes it fast and reliable.
#
# The code is also written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_gopt_is_java_home=false
_gopt_is_java_opt=false
_gopt_is_gradle_opt=false

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_2_=""
while [ $# -gt 0 ]; do
    case "$1" in
        --java-home)
            _gopt_is_java_home=true
            shift
            if [ $# -eq 0 ]; then
                die "ERROR: --java-home requires an argument"
            fi
            ;;
        --java-home=*)
            _gopt_java_home="${1#*=}"
            _gopt_java_home_set=true
            shift
            ;;
        -D*)
            _GOPT_JAVA_OPTS_="$_GOPT_JAVA_OPTS_ $1"
            shift
            ;;
        -J*)
            _GOPT_JAVA_OPTS_="$_GOPT_JAVA_OPTS_ ${1#-J}"
            shift
            ;;
        *)
            _GOPT_APP_ARGS_2_="$_GOPT_APP_ARGS_2_ \"$1\""
            shift
            ;;
    esac
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_2_" ]; then
    eval "set -- $_GOPT_APP_ARGS_2_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if $_gopt_java_home; then
    if [ $# -eq 0 ]; then
        die "ERROR: --java-home requires an argument"
    fi
    JAVA_HOME="$1"
    shift
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if $_gopt_java_home_set; then
    JAVA_HOME="$_gopt_java_home"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -z "$JAVA_HOME" ]; then
    die "ERROR: JAVA_HOME is not set and no 'java' command could be found in your PATH.

Please set the JAVA_HOME variable in your environment to match the
location of your Java installation."
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_3_=""
while [ $# -gt 0 ]; do
    case "$1" in
        --java-home)
            shift
            if [ $# -eq 0 ]; then
                die "ERROR: --java-home requires an argument"
            fi
            shift
            ;;
        --java-home=*)
            shift
            ;;
        -D*)
            shift
            ;;
        -J*)
            shift
            ;;
        *)
            _GOPT_APP_ARGS_3_="$_GOPT_APP_ARGS_3_ \"$1\""
            shift
            ;;
    esac
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_3_" ]; then
    eval "set -- $_GOPT_APP_ARGS_3_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_4_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_4_="$_GOPT_APP_ARGS_4_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_4_" ]; then
    eval "set -- $_GOPT_APP_ARGS_4_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_5_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_5_="$_GOPT_APP_ARGS_5_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_5_" ]; then
    eval "set -- $_GOPT_APP_ARGS_5_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_6_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_6_="$_GOPT_APP_ARGS_6_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_6_" ]; then
    eval "set -- $_GOPT_APP_ARGS_6_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_7_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_7_="$_GOPT_APP_ARGS_7_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_7_" ]; then
    eval "set -- $_GOPT_APP_ARGS_7_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_8_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_8_="$_GOPT_APP_ARGS_8_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_8_" ]; then
    eval "set -- $_GOPT_APP_ARGS_8_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_9_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_9_="$_GOPT_APP_ARGS_9_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_9_" ]; then
    eval "set -- $_GOPT_APP_ARGS_9_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_10_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_10_="$_GOPT_APP_ARGS_10_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_10_" ]; then
    eval "set -- $_GOPT_APP_ARGS_10_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_11_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_11_="$_GOPT_APP_ARGS_11_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_11_" ]; then
    eval "set -- $_GOPT_APP_ARGS_11_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_12_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_12_="$_GOPT_APP_ARGS_12_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_12_" ]; then
    eval "set -- $_GOPT_APP_ARGS_12_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_13_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_13_="$_GOPT_APP_ARGS_13_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_13_" ]; then
    eval "set -- $_GOPT_APP_ARGS_13_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_14_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_14_="$_GOPT_APP_ARGS_14_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_14_" ]; then
    eval "set -- $_GOPT_APP_ARGS_14_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_15_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_15_="$_GOPT_APP_ARGS_15_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_15_" ]; then
    eval "set -- $_GOPT_APP_ARGS_15_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_16_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_16_="$_GOPT_APP_ARGS_16_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_16_" ]; then
    eval "set -- $_GOPT_APP_ARGS_16_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_17_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_17_="$_GOPT_APP_ARGS_17_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_17_" ]; then
    eval "set -- $_GOPT_APP_ARGS_17_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_18_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_18_="$_GOPT_APP_ARGS_18_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_18_" ]; then
    eval "set -- $_GOPT_APP_ARGS_18_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_19_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_19_="$_GOPT_APP_ARGS_19_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_19_" ]; then
    eval "set -- $_GOPT_APP_ARGS_19_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_20_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_20_="$_GOPT_APP_ARGS_20_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_20_" ]; then
    eval "set -- $_GOPT_APP_ARGS_20_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_21_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_21_="$_GOPT_APP_ARGS_21_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_21_" ]; then
    eval "set -- $_GOPT_APP_ARGS_21_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_22_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_22_="$_GOPT_APP_ARGS_22_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_22_" ]; then
    eval "set -- $_GOPT_APP_ARGS_22_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_23_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_23_="$_GOPT_APP_ARGS_23_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_23_" ]; then
    eval "set -- $_GOPT_APP_ARGS_23_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_24_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_24_="$_GOPT_APP_ARGS_24_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_24_" ]; then
    eval "set -- $_GOPT_APP_ARGS_24_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_25_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_25_="$_GOPT_APP_ARGS_25_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_25_" ]; then
    eval "set -- $_GOPT_APP_ARGS_25_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_26_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_26_="$_GOPT_APP_ARGS_26_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_26_" ]; then
    eval "set -- $_GOPT_APP_ARGS_26_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_27_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_27_="$_GOPT_APP_ARGS_27_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_27_" ]; then
    eval "set -- $_GOPT_APP_ARGS_27_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_28_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_28_="$_GOPT_APP_ARGS_28_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_28_" ]; then
    eval "set -- $_GOPT_APP_ARGS_28_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_29_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_29_="$_GOPT_APP_ARGS_29_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_29_" ]; then
    eval "set -- $_GOPT_APP_ARGS_29_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_30_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_30_="$_GOPT_APP_ARGS_30_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_30_" ]; then
    eval "set -- $_GOPT_APP_ARGS_30_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_31_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_31_="$_GOPT_APP_ARGS_31_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_31_" ]; then
    eval "set -- $_GOPT_APP_ARGS_31_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_32_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_32_="$_GOPT_APP_ARGS_32_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_32_" ]; then
    eval "set -- $_GOPT_APP_ARGS_32_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_33_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_33_="$_GOPT_APP_ARGS_33_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_33_" ]; then
    eval "set -- $_GOPT_APP_ARGS_33_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_34_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_34_="$_GOPT_APP_ARGS_34_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_34_" ]; then
    eval "set -- $_GOPT_APP_ARGS_34_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_35_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_35_="$_GOPT_APP_ARGS_35_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_35_" ]; then
    eval "set -- $_GOPT_APP_ARGS_35_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_36_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_36_="$_GOPT_APP_ARGS_36_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_36_" ]; then
    eval "set -- $_GOPT_APP_ARGS_36_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_37_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_37_="$_GOPT_APP_ARGS_37_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_37_" ]; then
    eval "set -- $_GOPT_APP_ARGS_37_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_38_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_38_="$_GOPT_APP_ARGS_38_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_38_" ]; then
    eval "set -- $_GOPT_APP_ARGS_38_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_39_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_39_="$_GOPT_APP_ARGS_39_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_39_" ]; then
    eval "set -- $_GOPT_APP_ARGS_39_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_40_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_40_="$_GOPT_APP_ARGS_40_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_40_" ]; then
    eval "set -- $_GOPT_APP_ARGS_40_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_41_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_41_="$_GOPT_APP_ARGS_41_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_41_" ]; then
    eval "set -- $_GOPT_APP_ARGS_41_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_42_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_42_="$_GOPT_APP_ARGS_42_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_42_" ]; then
    eval "set -- $_GOPT_APP_ARGS_42_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_43_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_43_="$_GOPT_APP_ARGS_43_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_43_" ]; then
    eval "set -- $_GOPT_APP_ARGS_43_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_44_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_44_="$_GOPT_APP_ARGS_44_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_44_" ]; then
    eval "set -- $_GOPT_APP_ARGS_44_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_45_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_45_="$_GOPT_APP_ARGS_45_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_45_" ]; then
    eval "set -- $_GOPT_APP_ARGS_45_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_46_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_46_="$_GOPT_APP_ARGS_46_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_46_" ]; then
    eval "set -- $_GOPT_APP_ARGS_46_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_47_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_47_="$_GOPT_APP_ARGS_47_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_47_" ]; then
    eval "set -- $_GOPT_APP_ARGS_47_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_48_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_48_="$_GOPT_APP_ARGS_48_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_48_" ]; then
    eval "set -- $_GOPT_APP_ARGS_48_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_49_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_49_="$_GOPT_APP_ARGS_49_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_49_" ]; then
    eval "set -- $_GOPT_APP_ARGS_49_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_50_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_50_="$_GOPT_APP_ARGS_50_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_50_" ]; then
    eval "set -- $_GOPT_APP_ARGS_50_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_51_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_51_="$_GOPT_APP_ARGS_51_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_51_" ]; then
    eval "set -- $_GOPT_APP_ARGS_51_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_52_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_52_="$_GOPT_APP_ARGS_52_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_52_" ]; then
    eval "set -- $_GOPT_APP_ARGS_52_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_53_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_53_="$_GOPT_APP_ARGS_53_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_53_" ]; then
    eval "set -- $_GOPT_APP_ARGS_53_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_54_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_54_="$_GOPT_APP_ARGS_54_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_54_" ]; then
    eval "set -- $_GOPT_APP_ARGS_54_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_55_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_55_="$_GOPT_APP_ARGS_55_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_55_" ]; then
    eval "set -- $_GOPT_APP_ARGS_55_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_56_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_56_="$_GOPT_APP_ARGS_56_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_56_" ]; then
    eval "set -- $_GOPT_APP_ARGS_56_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_57_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_57_="$_GOPT_APP_ARGS_57_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_57_" ]; then
    eval "set -- $_GOPT_APP_ARGS_57_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_58_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_58_="$_GOPT_APP_ARGS_58_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_58_" ]; then
    eval "set -- $_GOPT_APP_ARGS_58_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_59_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_59_="$_GOPT_APP_ARGS_59_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_59_" ]; then
    eval "set -- $_GOPT_APP_ARGS_59_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_60_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_60_="$_GOPT_APP_ARGS_60_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_60_" ]; then
    eval "set -- $_GOPT_APP_ARGS_60_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_61_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_61_="$_GOPT_APP_ARGS_61_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_61_" ]; then
    eval "set -- $_GOPT_APP_ARGS_61_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_62_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_62_="$_GOPT_APP_ARGS_62_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_62_" ]; then
    eval "set -- $_GOPT_APP_ARGS_62_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_63_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_63_="$_GOPT_APP_ARGS_63_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_63_" ]; then
    eval "set -- $_GOPT_APP_ARGS_63_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_64_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_64_="$_GOPT_APP_ARGS_64_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_64_" ]; then
    eval "set -- $_GOPT_APP_ARGS_64_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_65_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_65_="$_GOPT_APP_ARGS_65_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_65_" ]; then
    eval "set -- $_GOPT_APP_ARGS_65_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_66_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_66_="$_GOPT_APP_ARGS_66_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_66_" ]; then
    eval "set -- $_GOPT_APP_ARGS_66_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_67_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_67_="$_GOPT_APP_ARGS_67_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_67_" ]; then
    eval "set -- $_GOPT_APP_ARGS_67_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_68_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_68_="$_GOPT_APP_ARGS_68_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_68_" ]; then
    eval "set -- $_GOPT_APP_ARGS_68_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_69_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_69_="$_GOPT_APP_ARGS_69_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_69_" ]; then
    eval "set -- $_GOPT_APP_ARGS_69_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_70_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_70_="$_GOPT_APP_ARGS_70_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_70_" ]; then
    eval "set -- $_GOPT_APP_ARGS_70_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_71_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_71_="$_GOPT_APP_ARGS_71_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_71_" ]; then
    eval "set -- $_GOPT_APP_ARGS_71_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_72_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_72_="$_GOPT_APP_ARGS_72_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_72_" ]; then
    eval "set -- $_GOPT_APP_ARGS_72_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_73_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_73_="$_GOPT_APP_ARGS_73_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_73_" ]; then
    eval "set -- $_GOPT_APP_ARGS_73_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_74_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_74_="$_GOPT_APP_ARGS_74_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_74_" ]; then
    eval "set -- $_GOPT_APP_ARGS_74_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_75_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_75_="$_GOPT_APP_ARGS_75_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_75_" ]; then
    eval "set -- $_GOPT_APP_ARGS_75_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_76_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_76_="$_GOPT_APP_ARGS_76_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_76_" ]; then
    eval "set -- $_GOPT_APP_ARGS_76_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_77_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_77_="$_GOPT_APP_ARGS_77_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_77_" ]; then
    eval "set -- $_GOPT_APP_ARGS_77_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_78_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_78_="$_GOPT_APP_ARGS_78_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_78_" ]; then
    eval "set -- $_GOPT_APP_ARGS_78_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_79_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_79_="$_GOPT_APP_ARGS_79_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_79_" ]; then
    eval "set -- $_GOPT_APP_ARGS_79_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_80_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_80_="$_GOPT_APP_ARGS_80_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_80_" ]; then
    eval "set -- $_GOPT_APP_ARGS_80_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_81_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_81_="$_GOPT_APP_ARGS_81_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_81_" ]; then
    eval "set -- $_GOPT_APP_ARGS_81_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_82_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_82_="$_GOPT_APP_ARGS_82_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_82_" ]; then
    eval "set -- $_GOPT_APP_ARGS_82_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_83_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_83_="$_GOPT_APP_ARGS_83_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_83_" ]; then
    eval "set -- $_GOPT_APP_ARGS_83_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_84_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_84_="$_GOPT_APP_ARGS_84_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_84_" ]; then
    eval "set -- $_GOPT_APP_ARGS_84_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_85_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_85_="$_GOPT_APP_ARGS_85_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_85_" ]; then
    eval "set -- $_GOPT_APP_ARGS_85_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_86_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_86_="$_GOPT_APP_ARGS_86_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_86_" ]; then
    eval "set -- $_GOPT_APP_ARGS_86_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_87_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_87_="$_GOPT_APP_ARGS_87_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_87_" ]; then
    eval "set -- $_GOPT_APP_ARGS_87_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_88_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_88_="$_GOPT_APP_ARGS_88_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_88_" ]; then
    eval "set -- $_GOPT_APP_ARGS_88_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_89_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_89_="$_GOPT_APP_ARGS_89_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_89_" ]; then
    eval "set -- $_GOPT_APP_ARGS_89_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_90_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_90_="$_GOPT_APP_ARGS_90_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_90_" ]; then
    eval "set -- $_GOPT_APP_ARGS_90_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_91_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_91_="$_GOPT_APP_ARGS_91_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_91_" ]; then
    eval "set -- $_GOPT_APP_ARGS_91_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_92_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_92_="$_GOPT_APP_ARGS_92_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_92_" ]; then
    eval "set -- $_GOPT_APP_ARGS_92_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_93_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_93_="$_GOPT_APP_ARGS_93_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_93_" ]; then
    eval "set -- $_GOPT_APP_ARGS_93_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_94_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_94_="$_GOPT_APP_ARGS_94_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_94_" ]; then
    eval "set -- $_GOPT_APP_ARGS_94_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_95_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_95_="$_GOPT_APP_ARGS_95_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_95_" ]; then
    eval "set -- $_GOPT_APP_ARGS_95_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_96_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_96_="$_GOPT_APP_ARGS_96_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_96_" ]; then
    eval "set -- $_GOPT_APP_ARGS_96_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_97_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_97_="$_GOPT_APP_ARGS_97_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_97_" ]; then
    eval "set -- $_GOPT_APP_ARGS_97_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_98_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_98_="$_GOPT_APP_ARGS_98_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_98_" ]; then
    eval "set -- $_GOPT_APP_ARGS_98_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_99_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_99_="$_GOPT_APP_ARGS_99_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_99_" ]; then
    eval "set -- $_GOPT_APP_ARGS_99_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_100_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_100_="$_GOPT_APP_ARGS_100_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_100_" ]; then
    eval "set -- $_GOPT_APP_ARGS_100_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_101_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_101_="$_GOPT_APP_ARGS_101_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_101_" ]; then
    eval "set -- $_GOPT_APP_ARGS_101_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_102_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_102_="$_GOPT_APP_ARGS_102_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_102_" ]; then
    eval "set -- $_GOPT_APP_ARGS_102_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_103_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_103_="$_GOPT_APP_ARGS_103_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_103_" ]; then
    eval "set -- $_GOPT_APP_ARGS_103_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_104_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_104_="$_GOPT_APP_ARGS_104_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_104_" ]; then
    eval "set -- $_GOPT_APP_ARGS_104_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_105_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_105_="$_GOPT_APP_ARGS_105_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_105_" ]; then
    eval "set -- $_GOPT_APP_ARGS_105_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_106_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_106_="$_GOPT_APP_ARGS_106_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_106_" ]; then
    eval "set -- $_GOPT_APP_ARGS_106_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_107_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_107_="$_GOPT_APP_ARGS_107_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_107_" ]; then
    eval "set -- $_GOPT_APP_ARGS_107_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_108_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_108_="$_GOPT_APP_ARGS_108_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_108_" ]; then
    eval "set -- $_GOPT_APP_ARGS_108_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_109_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_109_="$_GOPT_APP_ARGS_109_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_109_" ]; then
    eval "set -- $_GOPT_APP_ARGS_109_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_110_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_110_="$_GOPT_APP_ARGS_110_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_110_" ]; then
    eval "set -- $_GOPT_APP_ARGS_110_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_111_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_111_="$_GOPT_APP_ARGS_111_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_111_" ]; then
    eval "set -- $_GOPT_APP_ARGS_111_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_112_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_112_="$_GOPT_APP_ARGS_112_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_112_" ]; then
    eval "set -- $_GOPT_APP_ARGS_112_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_113_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_113_="$_GOPT_APP_ARGS_113_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_113_" ]; then
    eval "set -- $_GOPT_APP_ARGS_113_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_114_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_114_="$_GOPT_APP_ARGS_114_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_114_" ]; then
    eval "set -- $_GOPT_APP_ARGS_114_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_115_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_115_="$_GOPT_APP_ARGS_115_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_115_" ]; then
    eval "set -- $_GOPT_APP_ARGS_115_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_116_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_116_="$_GOPT_APP_ARGS_116_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_116_" ]; then
    eval "set -- $_GOPT_APP_ARGS_116_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_117_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_117_="$_GOPT_APP_ARGS_117_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_117_" ]; then
    eval "set -- $_GOPT_APP_ARGS_117_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_118_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_118_="$_GOPT_APP_ARGS_118_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_118_" ]; then
    eval "set -- $_GOPT_APP_ARGS_118_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_119_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_119_="$_GOPT_APP_ARGS_119_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_119_" ]; then
    eval "set -- $_GOPT_APP_ARGS_119_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_120_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_120_="$_GOPT_APP_ARGS_120_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_120_" ]; then
    eval "set -- $_GOPT_APP_ARGS_120_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_121_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_121_="$_GOPT_APP_ARGS_121_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_121_" ]; then
    eval "set -- $_GOPT_APP_ARGS_121_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_122_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_122_="$_GOPT_APP_ARGS_122_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_122_" ]; then
    eval "set -- $_GOPT_APP_ARGS_122_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_123_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_123_="$_GOPT_APP_ARGS_123_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_123_" ]; then
    eval "set -- $_GOPT_APP_ARGS_123_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_124_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_124_="$_GOPT_APP_ARGS_124_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_124_" ]; then
    eval "set -- $_GOPT_APP_ARGS_124_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_125_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_125_="$_GOPT_APP_ARGS_125_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_125_" ]; then
    eval "set -- $_GOPT_APP_ARGS_125_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_126_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_126_="$_GOPT_APP_ARGS_126_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_126_" ]; then
    eval "set -- $_GOPT_APP_ARGS_126_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_127_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_127_="$_GOPT_APP_ARGS_127_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_127_" ]; then
    eval "set -- $_GOPT_APP_ARGS_127_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_128_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_128_="$_GOPT_APP_ARGS_128_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_128_" ]; then
    eval "set -- $_GOPT_APP_ARGS_128_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_129_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_129_="$_GOPT_APP_ARGS_129_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_129_" ]; then
    eval "set -- $_GOPT_APP_ARGS_129_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_130_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_130_="$_GOPT_APP_ARGS_130_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_130_" ]; then
    eval "set -- $_GOPT_APP_ARGS_130_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_131_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_131_="$_GOPT_APP_ARGS_131_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_131_" ]; then
    eval "set -- $_GOPT_APP_ARGS_131_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_132_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_132_="$_GOPT_APP_ARGS_132_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_132_" ]; then
    eval "set -- $_GOPT_APP_ARGS_132_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_133_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_133_="$_GOPT_APP_ARGS_133_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_133_" ]; then
    eval "set -- $_GOPT_APP_ARGS_133_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_134_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_134_="$_GOPT_APP_ARGS_134_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_134_" ]; then
    eval "set -- $_GOPT_APP_ARGS_134_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_135_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_135_="$_GOPT_APP_ARGS_135_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_135_" ]; then
    eval "set -- $_GOPT_APP_ARGS_135_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_136_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_136_="$_GOPT_APP_ARGS_136_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_136_" ]; then
    eval "set -- $_GOPT_APP_ARGS_136_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_137_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_137_="$_GOPT_APP_ARGS_137_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_137_" ]; then
    eval "set -- $_GOPT_APP_ARGS_137_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_138_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_138_="$_GOPT_APP_ARGS_138_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_138_" ]; then
    eval "set -- $_GOPT_APP_ARGS_138_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_139_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_139_="$_GOPT_APP_ARGS_139_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_139_" ]; then
    eval "set -- $_GOPT_APP_ARGS_139_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_140_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_140_="$_GOPT_APP_ARGS_140_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_140_" ]; then
    eval "set -- $_GOPT_APP_ARGS_140_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_141_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_141_="$_GOPT_APP_ARGS_141_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_141_" ]; then
    eval "set -- $_GOPT_APP_ARGS_141_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_142_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_142_="$_GOPT_APP_ARGS_142_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_142_" ]; then
    eval "set -- $_GOPT_APP_ARGS_142_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_143_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_143_="$_GOPT_APP_ARGS_143_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_143_" ]; then
    eval "set -- $_GOPT_APP_ARGS_143_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_144_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_144_="$_GOPT_APP_ARGS_144_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_144_" ]; then
    eval "set -- $_GOPT_APP_ARGS_144_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_145_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_145_="$_GOPT_APP_ARGS_145_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_145_" ]; then
    eval "set -- $_GOPT_APP_ARGS_145_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_146_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_146_="$_GOPT_APP_ARGS_146_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_146_" ]; then
    eval "set -- $_GOPT_APP_ARGS_146_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_147_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_147_="$_GOPT_APP_ARGS_147_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_147_" ]; then
    eval "set -- $_GOPT_APP_ARGS_147_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_148_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_148_="$_GOPT_APP_ARGS_148_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_148_" ]; then
    eval "set -- $_GOPT_APP_ARGS_148_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_149_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_149_="$_GOPT_APP_ARGS_149_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_149_" ]; then
    eval "set -- $_GOPT_APP_ARGS_149_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_150_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_150_="$_GOPT_APP_ARGS_150_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_150_" ]; then
    eval "set -- $_GOPT_APP_ARGS_150_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_151_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_151_="$_GOPT_APP_ARGS_151_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_151_" ]; then
    eval "set -- $_GOPT_APP_ARGS_151_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_152_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_152_="$_GOPT_APP_ARGS_152_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_152_" ]; then
    eval "set -- $_GOPT_APP_ARGS_152_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_153_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_153_="$_GOPT_APP_ARGS_153_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_153_" ]; then
    eval "set -- $_GOPT_APP_ARGS_153_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_154_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_154_="$_GOPT_APP_ARGS_154_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_154_" ]; then
    eval "set -- $_GOPT_APP_ARGS_154_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_155_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_155_="$_GOPT_APP_ARGS_155_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_155_" ]; then
    eval "set -- $_GOPT_APP_ARGS_155_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_156_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_156_="$_GOPT_APP_ARGS_156_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_156_" ]; then
    eval "set -- $_GOPT_APP_ARGS_156_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_157_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_157_="$_GOPT_APP_ARGS_157_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_157_" ]; then
    eval "set -- $_GOPT_APP_ARGS_157_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_158_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_158_="$_GOPT_APP_ARGS_158_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_158_" ]; then
    eval "set -- $_GOPT_APP_ARGS_158_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_159_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_159_="$_GOPT_APP_ARGS_159_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_159_" ]; then
    eval "set -- $_GOPT_APP_ARGS_159_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_160_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_160_="$_GOPT_APP_ARGS_160_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_160_" ]; then
    eval "set -- $_GOPT_APP_ARGS_160_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_161_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_161_="$_GOPT_APP_ARGS_161_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_161_" ]; then
    eval "set -- $_GOPT_APP_ARGS_161_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_162_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_162_="$_GOPT_APP_ARGS_162_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_162_" ]; then
    eval "set -- $_GOPT_APP_ARGS_162_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_163_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_163_="$_GOPT_APP_ARGS_163_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_163_" ]; then
    eval "set -- $_GOPT_APP_ARGS_163_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_164_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_164_="$_GOPT_APP_ARGS_164_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_164_" ]; then
    eval "set -- $_GOPT_APP_ARGS_164_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_165_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_165_="$_GOPT_APP_ARGS_165_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_165_" ]; then
    eval "set -- $_GOPT_APP_ARGS_165_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_166_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_166_="$_GOPT_APP_ARGS_166_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_166_" ]; then
    eval "set -- $_GOPT_APP_ARGS_166_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_167_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_167_="$_GOPT_APP_ARGS_167_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_167_" ]; then
    eval "set -- $_GOPT_APP_ARGS_167_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_168_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_168_="$_GOPT_APP_ARGS_168_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_168_" ]; then
    eval "set -- $_GOPT_APP_ARGS_168_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_169_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_169_="$_GOPT_APP_ARGS_169_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_169_" ]; then
    eval "set -- $_GOPT_APP_ARGS_169_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_170_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_170_="$_GOPT_APP_ARGS_170_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_170_" ]; then
    eval "set -- $_GOPT_APP_ARGS_170_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_171_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_171_="$_GOPT_APP_ARGS_171_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_171_" ]; then
    eval "set -- $_GOPT_APP_ARGS_171_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_172_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_172_="$_GOPT_APP_ARGS_172_"
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_172_" ]; then
    eval "set -- $_GOPT_APP_ARGS_172_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_173_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_173_="$_GOPT_APP_ARGS_173_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_173_" ]; then
    eval "set -- $_GOPT_APP_ARGS_173_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_174_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_174_="$_GOPT_APP_ARGS_174_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_174_" ]; then
    eval "set -- $_GOPT_APP_ARGS_174_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_175_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_175_="$_GOPT_APP_ARGS_175_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_175_" ]; then
    eval "set -- $_GOPT_APP_ARGS_175_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_176_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_176_="$_GOPT_APP_ARGS_176_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_176_" ]; then
    eval "set -- $_GOPT_APP_ARGS_176_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_177_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_177_="$_GOPT_APP_ARGS_177_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_177_" ]; then
    eval "set -- $_GOPT_APP_ARGS_177_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_178_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_178_="$_GOPT_APP_ARGS_178_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_178_" ]; then
    eval "set -- $_GOPT_APP_ARGS_178_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most performant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_179_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_179_="$_GOPT_APP_ARGS_179_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_179_" ]; then
    eval "set -- $_GOPT_APP_ARGS_179_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_180_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_180_="$_GOPT_APP_ARGS_180_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_180_" ]; then
    eval "set -- $_GOPT_APP_ARGS_180_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_181_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_181_="$_GOPT_APP_ARGS_181_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_181_" ]; then
    eval "set -- $_GOPT_APP_ARGS_181_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_182_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_182_="$_GOPT_APP_ARGS_182_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_182_" ]; then
    eval "set -- $_GOPT_APP_ARGS_182_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_183_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_183_="$_GOPT_APP_ARGS_183_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_183_" ]; then
    eval "set -- $_GOPT_APP_ARGS_183_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_184_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_184_="$_GOPT_APP_ARGS_184_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_184_" ]; then
    eval "set -- $_GOPT_APP_ARGS_184_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_185_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_185_="$_GOPT_APP_ARGS_185_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_185_" ]; then
    eval "set -- $_GOPT_APP_ARGS_185_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_186_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_186_="$_GOPT_APP_ARGS_186_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_186_" ]; then
    eval "set -- $_GOPT_APP_ARGS_186_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_187_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_187_="$_GOPT_APP_ARGS_187_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_187_" ]; then
    eval "set -- $_GOPT_APP_ARGS_187_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_188_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_188_="$_GOPT_APP_ARGS_188_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_188_" ]; then
    eval "set -- $_GOPT_APP_ARGS_188_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_189_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_189_="$_GOPT_APP_ARGS_189_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_189_" ]; then
    eval "set -- $_GOPT_APP_ARGS_189_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_190_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_190_="$_GOPT_APP_ARGS_190_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_190_" ]; then
    eval "set -- $_GOPT_APP_ARGS_190_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_191_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_191_="$_GOPT_APP_ARGS_191_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_191_" ]; then
    eval "set -- $_GOPT_APP_ARGS_191_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_192_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_192_="$_GOPT_APP_ARGS_192_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_192_" ]; then
    eval "set -- $_GOPT_APP_ARGS_192_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_193_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_193_="$_GOPT_APP_ARGS_193_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_193_" ]; then
    eval "set -- $_GOPT_APP_ARGS_193_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_194_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_194_="$_GOPT_APP_ARGS_194_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_194_" ]; then
    eval "set -- $_GOPT_APP_ARGS_194_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_195_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_195_="$_GOPT_APP_ARGS_195_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_195_" ]; then
    eval "set -- $_GOPT_APP_ARGS_195_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_196_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_196_="$_GOPT_APP_ARGS_196_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_196_" ]; then
    eval "set -- $_GOPT_APP_ARGS_196_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_197_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_197_="$_GOPT_APP_ARGS_197_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_197_" ]; then
    eval "set -- $_GOPT_APP_ARGS_197_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_198_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_198_="$_GOPT_APP_ARGS_198_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_198_" ]; then
    eval "set -- $_GOPT_APP_ARGS_198_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_199_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_199_="$_GOPT_APP_ARGS_199_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_199_" ]; then
    eval "set -- $_GOPT_APP_ARGS_199_"
fi

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
_GOPT_APP_ARGS_200_=""
for _gopt_arg in "$@"; do
    _GOPT_APP_ARGS_200_="$_GOPT_APP_ARGS_200_ \"$_gopt_arg\""
done

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
if [ -n "$_GOPT_APP_ARGS_200_" ]; then
    eval "set -- $_GOPT_APP_ARGS_200_"
fi

CLASSPATH="$APP_HOME/gradle/wrapper/gradle-wrapper.jar"

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
JAVACMD="$JAVA_HOME/bin/java"

# The following code is generated by `gradlew-java-opts-parser.js`
#
# The code is written in a way that it is easy to follow and debug.
# It is not the most perform-ant way to parse the arguments, but it is reliable.
#
# The code is also written in a way that it can be used in shells that do not support arrays.
# This means that we have to resort to string concatenation and eval.
#
# The code is generated by `gradlew-java-opts-parser.js`
#
eval exec "\"$JAVACMD\"" $DEFAULT_JVM_OPTS $JAVA_OPTS $GRADLE_OPTS "-Dorg.gradle.appname=$APP_BASE_NAME" -classpath "\"$CLASSPATH\"" org.gradle.wrapper.GradleWrapperMain "$@"
fi
