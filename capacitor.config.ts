
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mealgenna.app',
  appName: 'MealGenna',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
