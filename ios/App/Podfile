# DO NOT MODIFY.
# This file is generated by Capacitor.
# If you a change to this file, you may have it overwritten in the future.

platform :ios, '13.0'

# Add this to the top of your Podfile
# to import React Native dependencies
# require_relative '../../node_modules/react-native/scripts/react_native_pods'

target 'App' do
  # Enables Swift support
  # use_frameworks!

  # Add this to your Podfile to use React Native
  # and import React dependencies
  # use_react_native!

  # Pods for App
  pod 'Capacitor', :path => '../../node_modules/@capacitor/ios'
  pod 'CapacitorCordova', :path => '../../node_modules/@capacitor/ios'
  pod 'RevenueCat/Purchases-Capacitor', :path => '../../node_modules/@revenuecat/purchases-capacitor'

  # Add this to your Podfile to import static libraries
  # and configure Flipper for React Native.
  # post_install do |installer|
  #   flipper_pods = {
  #     'Flipper' => '0.195.0',
  #     'Flipper-DoubleConversion' => '3.2.1',
  #     'Flipper-Folly' => '2.6.10',
  #     'Flipper-Glog' => '0.6.0',
  #     'Flipper-PeerTalk' => '0.0.4',
  #     'Flipper-RSocket' => '1.4.3'
  #   }
  #   flipper_pods.each do |name, version|
  #     installer.pods_project.pod_targets.select { |target| target.name == name }.each do |target|
  #       target.build_configurations.each do |config|
  #         config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
  #       end
  #     end
  #   end
  #   installer.pods_project.targets.each do |target|
  #     if target.name == 'React-Core'
  #       target.build_configurations.each do |config|
  #         config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
  #       end
  #     end
  #   end
  # end
end

# Do not add pods here.
# Instead, add them to your target 'App' above.
# post_install do |installer|
#   installer.pods_project.targets.each do |target|
#     target.build_configurations.each do |config|
#       # You can remove this workaround after minimum deployment target is 13.0
#       config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
#     end
#   end
# end
