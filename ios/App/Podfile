# DO NOT MODIFY. This file is generated by Capacitor.
# If you want to add a new dependency, add it to ios/App/Podfile.
# For more information, see: https://capacitorjs.com/docs/ios/podfile

# platform :ios, '13.0'
use_frameworks!

# Fix for duplicate source files error in Xcode 12
install! 'cocoapods', :disable_input_output_paths => true

target 'App' do
  # Do not change to static frameworks. It will break the build.
  # https://github.com/ionic-team/capacitor/issues/3834
  use_frameworks!
  # Enables Swift support
  use_native_modules!

  # Pods for App
  pod 'Capacitor', :path => '../../node_modules/@capacitor/ios'
  pod 'CapacitorCordova', :path => '../../node_modules/@capacitor/ios'
  pod 'PurchasesHybridCommon', '~> 9.3.0'
  pod 'PurchasesCapacitor', :path => '../../node_modules/@revenuecat/purchases-capacitor'

  # Add your Pods here

end

post_install do |installer|
  # Capacitor-specific logic
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      # Enable Swift Lint: https://capacitorjs.com/docs/ios/guides/swift-lint
      # config.build_settings['SWIFT_ACTIVE_COMPILATION_CONDITIONS'] ||= ['$(inherited)']
      # config.build_settings['SWIFT_ACTIVE_COMPILATION_CONDITIONS'] << 'SWIFT_LINT'

      # The new build system has issues with Capacitor projects, this is a workaround
      # https://github.com/ionic-team/capacitor/issues/4079
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'

      # Workaround for Cocoapods + Swift an issue where development pods are not compiled for ARCHS_STANDARD
      # https://github.com/CocoaPods/CocoaPods/issues/8206
      config.build_settings['VALID_ARCHS'] = 'arm64'
    end
  end
end
