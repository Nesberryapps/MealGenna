# DO NOT MODIFY.
# This file is generated by Capacitor.
# If you need to make changes, follow the instructions here:
# https://capacitorjs.com/docs/custom-code/native-platforms

platform :ios, '13.0'
use_frameworks!

# Require the pods of all plugins
require_relative '../../node_modules/@capacitor/ios/scripts/pods'

target 'App' do
  capacitor_pods
  # Add your Pods here
end

post_install do |installer|
  # Fixes an issue with Xcode 12.5
  # https://github.com/ionic-team/capacitor/issues/4073
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
    end
  end
end
